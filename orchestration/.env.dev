# Development Environment Configuration
# Matches Coolify dev configuration
ENVIRONMENT=development

# Service Names (with -dev suffix)
BACKEND_SERVICE_NAME=fc-backend-dev
FRONTEND_SERVICE_NAME=fc-frontend-dev
SCRAPER_SERVICE_NAME=scraper-dev

# Service Hosts (internal Docker network communication)
BACKEND_HOST=backend
FRONTEND_HOST=frontend
SCRAPER_HOST=scraper

# Ports (matches Coolify dev: 5070/5071/3010)
BACKEND_PORT=5070
FRONTEND_PORT=5071
SCRAPER_PORT=3010

# Service URLs (internal Docker network)
SCRAPER_SERVICE_URL=http://scraper:3010

# Database
MONGODB_URI=mongodb://mongodb:27017/figure-collector-dev

# JWT Secrets (32+ characters minimum)
# Dev secrets - use openssl rand -base64 32 for production
JWT_SECRET=dev-jwt-secret-at-least-32-characters-long-1234567890
JWT_REFRESH_SECRET=dev-refresh-secret-at-least-32-characters-long-123456

# Service Authentication Token (for inter-service communication)
SERVICE_AUTH_TOKEN=dev-service-auth-token-change-in-production

# React App Configuration (build-time variables for frontend)
REACT_APP_API_URL=http://localhost:5070

# Docker Registry (not needed for local dev build)
REGISTRY_URL=ghcr.io/rpgoldberg

# Image Tags (not used in dev - we build from source)
BACKEND_TAG=v2.0.1
FRONTEND_TAG=v2.0.1
SCRAPER_TAG=v2.0.1
