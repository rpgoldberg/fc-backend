# Production Environment Configuration
ENVIRONMENT=production

# Service Names (no suffix for prod)
BACKEND_SERVICE_NAME=fc-backend
FRONTEND_SERVICE_NAME=fc-frontend
SCRAPER_SERVICE_NAME=scraper

# Service Hosts (internal container communication)
BACKEND_HOST=fc-backend
FRONTEND_HOST=fc-frontend
SCRAPER_HOST=scraper

# Ports
BACKEND_PORT=5050
FRONTEND_PORT=5051
SCRAPER_PORT=3050

# URLs (inter-service communication)
SCRAPER_SERVICE_URL=http://scraper:3050
# BACKEND_URL is set on the SCRAPER container for webhook callbacks to backend
BACKEND_URL=http://fc-backend:5050

# Database & Auth (set these for each environment)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/figure-collector?retryWrites=true&w=majority
JWT_SECRET=your_jwt_secret_here_prod

# Docker Registry (for compose)
REGISTRY_URL=your-registry-url

# Tags
BACKEND_TAG=v2.0.1
FRONTEND_TAG=v2.0.1
SCRAPER_TAG=v2.0.1