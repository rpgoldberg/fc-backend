# Environment Configuration Example
# Copy this file to .env.dev, .env.test, or .env.prod and customize the values

# Environment type
ENVIRONMENT=development

# Service Names
# Dev: add -dev suffix, Test: add -test suffix, Prod: no suffix
BACKEND_SERVICE_NAME=fc-backend-dev
FRONTEND_SERVICE_NAME=fc-frontend-dev
SCRAPER_SERVICE_NAME=scraper-dev

# Service Hosts (internal container communication)
BACKEND_HOST=fc-backend-dev
FRONTEND_HOST=fc-frontend-dev
SCRAPER_HOST=scraper-dev

# Ports
# Dev: 5070/5071/3010, Test: 5055/5056/3005, Prod: 5050/5051/3000
BACKEND_PORT=5070
FRONTEND_PORT=5071
SCRAPER_PORT=3010

# URLs
SCRAPER_SERVICE_URL=http://scraper-dev:3010

# Database & Auth (MUST be customized for each environment)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/figure-collector-dev?retryWrites=true&w=majority
# JWT Secrets (REQUIRED - Must be at least 32 characters in production)
# Generate secure secrets using: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_here_must_be_at_least_32_characters_long
JWT_REFRESH_SECRET=your_refresh_secret_here_must_be_at_least_32_chars

# Service Authentication Token (REQUIRED for inter-service communication)
SERVICE_AUTH_TOKEN=your_service_auth_token_here

# Docker Registry (for compose deployments)
REGISTRY_URL=your-registry-url

# Image Tags (use specific versions for production)
BACKEND_TAG=v2.0.1
FRONTEND_TAG=v2.0.1
SCRAPER_TAG=v2.0.1

# Cloudflare Tunnel (if used)
TUNNEL_ID=your_tunnel_id
DOMAIN_NAME=your.domain.com
